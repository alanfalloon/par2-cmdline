#!/usr/bin/make -f

include /usr/share/cdbs/1/rules/debhelper.mk
include /usr/share/cdbs/1/class/autotools.mk

$(DEB_BUILDDIR)/config.status: debian/stamp-bootstrap
debian/stamp-bootstrap: bootstrap
	sh -e -x $<
	touch $@

clean::
	rm -f debian/stamp-bootstrap
